{
  "name": "otel-nestjs-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^1.11.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
